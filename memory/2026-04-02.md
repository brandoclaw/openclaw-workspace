
## Session Update — Apr 2, 2026 (afternoon)

### Self-Review P1–P6 Completed
- P1: AGENTS.md — Maximus job search cron + flow header marked ⏸️ PAUSED as of Mar 31
- P2: MEMORY.md — Jacob trigger corrected to `Status = Paid`; approvers updated to real Telegram IDs (Brando `8776720992`, Tim `8720480894`)
- P3: AGENTS.md — Jacob workflow section added (full 7-step pipeline)
- P4: MEMORY.md — Drive upload confirmed working end-to-end ✅
- P5: TOOLS.md — `workspace-trendpulse` reference replaced with `workspace-jacob`
- P6: MEMORY.md — "Recent Activity (Apr 1)" ephemeral section absorbed + removed; `_Last updated` bumped to 2026-04-02
- P7: Ottawa Weekly Issue #5 — Brando will review

### Jacob — Fully Live ✅
- Airtable webhook automation confirmed in place (Apr 2)
- Trigger: Status = Paid → POST to `https://subclavate-deacon-isogeothermic.ngrok-free.dev/webhook`
- Both approvers configured: Brando + Tim (chat ID `8720480894`)
- Drive upload confirmed working
- ngrok + webhook server auto-start via launchd on boot
- Manual order entry in Airtable → mark as Paid → Jacob fires automatically

### State as of end of session
- Jacob: fully operational, webhook live, auto-start configured
- Maximus job search: paused indefinitely
- Ottawa Weekly: Issue #5 on Drive, Brando reviewing
- All core docs (MEMORY.md, AGENTS.md, SOUL.md, TOOLS.md) up to date

### Jacob Webhook Bug Fix (4:00–4:40 PM EDT)
Two bugs causing pipeline to silently not fire:
1. `recordId` (camelCase) wasn't being extracted from Airtable payload — code only checked `record.id`, `id`, `records[0].id`
2. Status field was read from webhook body — but Airtable only sends `{"recordId": "GDA..."}`, not the full record

Fixes:
- `webhook_server.py`: added `recordId` camelCase handling in `_extract_record_id()`
- `airtable_client.py`: added `find_record_by_order_id()` — searches by Order ID formula field to get rec ID
- `webhook_server.py`: after extracting order ID, now calls `find_record_by_order_id()` then fetches full record to check Status

Confirmed working end-to-end with test trigger. Suno generated songs for Outlaws (recePpwR7PcH2I3VY) and sent Telegram approval.
